#include <gtest/gtest.h>

#include "verilated-input-signal.h"
#include "verilated-output-signal.h"
#include "verilated-signal-test.h"

namespace cores {
  TEST_F(VerilatedSignalTest, Expect) {
    // input_signal.Set(true);

    // fixtures.eval();

    // output_signal.Expect(true);
  }
}